{# src/eveg/BiblioBundle/Resources/views/Default/search.html.twig #}

{% extends "::layout.html.twig" %}


{% block title %}
	Ajouter une référence | Biblio | {{ parent() }}
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block meta%}
	{#<meta name="description" content="{{ 'eveg.meta.biblio.search'|trans }}" />#}
{% endblock %}

{% block body %}

	{{ parent() }}
	<div class="row whiteRow">
		<div class="jumbotron biblio">
			<p><b>Ajouter une référence bibliographique</b>
		</div>
	</div>

	{# Page #}
	<div class="row">
		<div class="col-md-offset-2 col-md-8">
			<div class="row">
				{{ form_start(form) }}

				{% if not form.vars.valid %}
					<br />
					{% for e in form.vars.errors %}
						<div class="alert alert-danger" role="alert">
							{{ e.message }}
						</div>
					{% endfor %}
				{% endif %}

				<ul style="margin: 0;padding: 0;">{{ form_row(form.reference) }}</ul>
				<br />
				<p><i>Vérifiez que la référence n'éxiste pas déjà.</i></p>
				<p><i>Si la référence existe mais est erronée, merci d'envoyer un mail à admin@e-veg.net plutôt que de créer une nouvelle référence.</i></p>
			</div>
			<div class="row">
				<div class="col-md-offset 4 col-md-4 col-md-offset-4">
					<div class="saveButtons">
						{{ form_widget(form.save) }}
					<div class="saveButtons">

					{{ form_widget(form._token) }}
					{{ form_end(form, {'render_rest': false}) }}
				</div>
			</div>
		</div>
	</div> <!-- ./row -->

{% endblock %}

{% block javascript %}
	{{ parent() }}
{% endblock %}