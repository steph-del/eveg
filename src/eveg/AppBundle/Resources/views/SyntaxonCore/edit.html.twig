{% extends "evegAppBundle:Admin:layout.html.twig" %}

{% block body -%}
    <h1>SyntaxonCore edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_syntaxon') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
